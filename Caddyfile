{
    auto_https off
}

:8080 {
    handle_path /api/* {
        reverse_proxy localhost:4000
    }

    handle {
        root * /app/frontend/dist
        try_files {path} /index.html
        file_server
    }
}
